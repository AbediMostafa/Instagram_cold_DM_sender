<?php

namespace Database\Seeders;

use App\Models\Account;
use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Storage;

class AccountSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
//        DB::table('accounts')->insert(json_decode(Storage::get('accounts.json'), true));
//        DB::table('templates')->insert(json_decode(Storage::get('templates.json'), true));
//        DB::table('leads')->insert(json_decode(Storage::get('leads.json'), true));
//        DB::table('proxies')->insert(json_decode(Storage::get('proxies.json'), true));
    }
}
